# 简易编译器 —— 编译原理期末答辩文档

> **学校**：重庆交通大学 &nbsp;|&nbsp; **课程**：编译原理 &nbsp;|&nbsp; **项目类型**：期末大作业

---

## 目录

- [一、项目概述](#一项目概述)
- [二、编译器架构总览](#二编译器架构总览)
- [三、编译全流程详解](#三编译全流程详解)
  - [阶段 1：词法分析（Lexical Analysis）](#阶段-1词法分析lexical-analysis)
  - [阶段 2：LL(1) 理论计算](#阶段-2ll1-理论计算)
  - [阶段 3：语法分析 + 中间代码生成](#阶段-3语法分析--中间代码生成)
  - [阶段 4：中间代码优化](#阶段-4中间代码优化)
  - [阶段 5：可视化报告](#阶段-5可视化报告)
- [四、关键数据结构](#四关键数据结构)
- [五、测试用例与运行结果](#五测试用例与运行结果)
- [六、答辩可能问到的问题（Q&A）](#六答辩可能问到的问题qa)
- [七、总结与收获](#七总结与收获)

---

## 一、项目概述

### 1.1 项目简介

本项目实现了一个**完整的编译器前端**，能够将 C/Java 风格的源代码经过词法分析、语法分析、语义分析、中间代码生成和代码优化，最终输出优化后的四元式中间代码和可视化 HTML 报告。

### 1.2 技术栈

| 维度 | 选型 |
|------|------|
| 开发语言 | Java（纯 JDK，零外部依赖） |
| 词法分析 | 手写 DFA（状态转换图） |
| 语法分析 | 递归下降分析法（LL(1) 理论支撑） |
| 中间表示 | 四元式（Quadruple） |
| 代码优化 | 常量合并 + 常量传播 |
| 可视化 | 纯前端 HTML + CSS + JavaScript |

### 1.3 核心功能清单

- [x] 词法分析：识别关键字、标识符、常量、运算符、界符，跳过注释，检测非法字符
- [x] LL(1) 理论：构造 FIRST 集、FOLLOW 集、预测分析表 M
- [x] 语法分析：递归下降解析，同步构造语法树和生成四元式
- [x] 语义检查：符号表管理，"先声明后使用"检查
- [x] 代码优化：常量合并（5*2→10）+ 常量传播（t1→10→10+10→20）
- [x] 可视化报告：5 选项卡交互式 HTML（词法/LL(1)/语法树/优化/最终代码）

---

## 二、编译器架构总览

### 2.1 整体流水线

```
源程序字符串 (String)
    │
    ▼
┌─────────────────┐
│  [1] 词法分析器   │  Lexer.java + Token.java + TokenType.java
│  逐字符扫描       │  输入："int a = 5;"
│  → Token 序列    │  输出：[KEYWORD:"int", IDENTIFIER:"a", OPERATOR:"=", INTEGER:"5", DELIMITER:";"]
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  [2] LL(1) 理论  │  LL1Analyzer.java + Production.java
│  文法→FIRST/FOLLOW│  FIRST(Expr) = {id, num}
│  → 预测分析表 M   │  FOLLOW(Expr) = {;, ), #}
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  [3] 语法分析器   │  Parser.java + TreeNode.java + Quadruple.java + SymbolTable.java
│  递归下降解析    │  输入：Token 序列
│  → 语法树 + 四元式│  输出：TreeNode 树根 + List<Quadruple>
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  [4] 代码优化器   │  Optimizer.java
│  常量合并+传播   │  (*,5,2,t1) → (=,10,_,t1)
│  → 优化后四元式   │  (+,10,10,t2) → (=,20,_,a)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  [5] 可视化报告   │  WebVisualizer.java
│  → HTML 网页     │  5 个选项卡，interactive
└─────────────────┘
```

### 2.2 文件结构

```
src/
├── Main.java                      # 程序入口，串联编译全流程
├── lexer/                         # 词法分析包
│   ├── TokenType.java             # 种别码枚举（9 种）
│   ├── Token.java                 # 二元式类 (type, value, line, col)
│   └── Lexer.java                 # 词法分析器核心（DFA 状态转换图）
├── parser/                        # 语法分析包
│   ├── SymbolTable.java           # 符号表（变量声明管理）
│   ├── Quadruple.java             # 四元式类 (op, arg1, arg2, result)
│   ├── Production.java            # 产生式类 (lhs → rhs)
│   ├── TreeNode.java              # 语法树节点（递归树结构）
│   ├── LL1Analyzer.java           # LL(1) 理论：FIRST/FOLLOW/预测分析表
│   ├── Parser.java                # 递归下降语法分析器 + 中间代码生成
│   └── Optimizer.java             # 中间代码优化器（常量合并+传播）
└── visualizer/
    └── WebVisualizer.java         # HTML 可视化报告生成器
```

### 2.3 类之间的关系

```
Main
 ├── Lexer → Token (TokenType)
 ├── LL1Analyzer → Production
 ├── Parser → Token, TreeNode, Quadruple, SymbolTable
 ├── Optimizer → Quadruple
 └── WebVisualizer → Token, LL1Analyzer, TreeNode, Quadruple
```

---

## 三、编译全流程详解

### 阶段 1：词法分析（Lexical Analysis）

**在教材中的位置**：§3.1-§3.4（词法分析、正规式、有限自动机）

#### 3.1.1 基本原理

词法分析是编译的第一个阶段。它的任务是把源程序字符串中的字符序列，转换为有意义的**单词（Token）**序列。这个过程本质上是在模拟一个**确定有限自动机（DFA）**。

#### 3.1.2 核心概念

**二元式**：每个单词用一个二元组 `<种别码, 属性值>` 表示。

| 种别码 | 说明 | 示例 |
|--------|------|------|
| KEYWORD | 关键字 | `int`, `if`, `while` |
| IDENTIFIER | 标识符 | `a`, `myVar`, `main` |
| INTEGER | 整数 | `5`, `42` |
| FLOAT | 浮点数 | `3.14` |
| STRING | 字符串 | `"hello"` |
| CHAR | 字符 | `'a'` |
| OPERATOR | 运算符 | `+`, `*`, `==`, `&&` |
| DELIMITER | 界符 | `(`, `{`, `;` |
| EOF | 文件结束 | （输入结束标志） |
| UNKNOWN | 非法字符 | （错误标记） |

#### 3.1.3 DFA 状态设计

词法分析器内部维护了 **9 个状态**：

```
                     ┌──────────┐
               ┌────→│  START   │←────┐
               │     └────┬─────┘     │
         字母/_│    数字  │  "  '  运算符 界符  /+字母
               ↓     ↓    ↓  ↓    ↓     ↓     ↓
          IN_ID  IN_INT  IN_STRING IN_CHAR IN_OP → 直接输出
            │      │                              │
            │  遇到'.'?                           │
            │      ↓                              │
            │  IN_FLOAT                           ↓
            ↓      ↓                         判断注释
          (返回Token)                   // → LINE_COMMENT
                                       /* → BLOCK_COMMENT
```

#### 3.1.4 关键算法

**识别标识符/关键字**：
```
while (当前字符是字母/数字/下划线) {
    读入字符
}
if (读到的字符串在关键字集合中)
    → KEYWORD
else
    → IDENTIFIER
```

**识别双字符运算符（前瞻一个字符）**：
```
读取第一个运算符字符 c
if (c + 下一个字符 是合法的双字符运算符)  // 如 "==", "!=", "&&"
    消耗第二个字符
    → 双字符运算符 Token
else
    → 单字符运算符 Token
```

**注释跳过**：注释在词法阶段被直接忽略，不产生任何 Token。`//` 跳到行尾，`/* */` 跳到闭合标记。

#### 3.1.5 答辩要点

1. **为什么不直接用正则表达式？** —— 手写 DFA 更灵活（可自定义错误恢复策略），且性能更好。正则引擎内部也是 DFA。
2. **"最长匹配"原则** —— 例如输入 `ifa`，虽然 `if` 是关键字，但因为后面跟了 `a` 且 `a` 是合法标识符字符，整体识别为标识符 `ifa`。
3. **为什么在词法阶段跳过注释？** —— 注释对程序语义无影响，在词法阶段忽略可以简化后续所有阶段。

---

### 阶段 2：LL(1) 理论计算

**在教材中的位置**：§4.3-§4.5（FIRST 集、FOLLOW 集、预测分析表）

#### 3.2.1 基本原理

LL(1) 是一种**自顶向下**的语法分析方法。它通过向前看**1 个**输入符号来决定使用哪条产生式。

- **第一个 L**：Left-to-right（从左到右扫描输入）
- **第二个 L**：Leftmost derivation（最左推导）
- **（1）**：One symbol lookahead（向前看一个符号）

#### 3.2.2 核心数据结构

**FIRST(α) 集合**：从 α 出发能推导出的所有**首终结符**的集合。

> 例如：FIRST(Expr) = {id, num}
> 意思是 Expr 推导出的句型总是以 id 或 num 开头。

**FOLLOW(A) 集合**：在所有句型中，紧跟在非终结符 A 之后的**终结符**的集合。

> 例如：FOLLOW(Expr) = {;, ), #}
> 意思是 Expr 后面总是跟 ; 或 ) 或输入结束符 #。

**预测分析表 M**：二维表 M[A, a]，表示当栈顶为非终结符 A、当前输入为终结符 a 时，应该使用的产生式。

> 例如：M[Expr, id] = "Expr → Term ExprP"
> 例如：M[Expr, +] = 空（表示语法错误）

#### 3.2.3 算法核心

**FIRST 集算法**（不动点迭代）：
```
初始化：每个非终结符的 FIRST 集为空
重复直到不再变化：
    对每条产生式 A → X₁X₂...：
        if X₁ 是终结符或 ε:
            将 X₁ 加入 FIRST(A)
        else:
            将 FIRST(X₁) 全部加入 FIRST(A)
```

**FOLLOW 集算法**（不动点迭代）：
```
初始化：FOLLOW(S) = {#}，其余为空
重复直到不再变化：
    对每条产生式：
        if 产生式是 A → αBβ:
            if β 是终结符: 加入 FOLLOW(B)
            else: FIRST(β)-{ε} 加入 FOLLOW(B)
        if 产生式是 A → αB（B 在末尾）:
            FOLLOW(A) 全部加入 FOLLOW(B)
```

**预测分析表构造**：
```
对每条产生式 A → α:
    对每个 a ∈ FIRST(α):
        M[A, a] = A → α
    if ε ∈ FIRST(α):
        对每个 b ∈ FOLLOW(A):
            M[A, b] = A → α（即 ε-产生式）
```

#### 3.2.4 本项目的文法

```
Program  → int main ( ) { StmtList }
StmtList → Stmt StmtList | ε
Stmt     → Decl | Assign
Decl     → int id ;
Assign   → id = Expr ;
Expr     → Term ExprP
ExprP    → + Term ExprP | ε
Term     → id | num
```

这是一个 LL(1) 文法（无左递归、无公共左因子），可以用预测分析法解析。

#### 3.2.5 答辩要点

1. **LL(1) vs LR(1)** —— LL(1) 是自顶向下，LR(1) 是自底向上。LL(1) 实现简单但能处理的文法范围较小。本项目用 LL(1) 是因为文法简单且递归下降实现直观。
2. **ε-产生式的作用** —— 表示"可以为空"。例如 StmtList → ε 表示语句列表可以为空（空函数体是合法的）。
3. **为什么预测分析表留空？** —— 空格表示该表项没有对应产生式，即出现了语法错误。例如 M[Program, id] 为空，因为程序必须以 `int main` 开头，直接写标识符是语法错误。

---

### 阶段 3：语法分析 + 中间代码生成

**在教材中的位置**：§4.0（自顶向下分析）、§7.2（四元式）、§6.2（符号表）

#### 3.3.1 实现方法：递归下降分析法

**核心思想**：为文法的每个非终结符编写一个解析函数。函数内根据当前 lookahead 符号选择产生式，然后按产生式右部顺序依次调用相应的函数。

#### 3.3.2 解析函数映射表

| 非终结符 | 解析函数 | 产生式 |
|----------|----------|--------|
| Program  | `parse()` | `int main () { StmtList }` |
| StmtList | `statementList()` | `Stmt StmtList \| ε` |
| Stmt     | `statement()` | `Decl \| Assign \| If \| While` |
| Decl     | `declaration()` | `int id ;` |
| Assign   | `assignment()` | `id = Expr ;` |
| Expr     | `expression()` | `Term { (+|-) Term }` |
| Term     | `term()` | `Factor { (*\|/) Factor }` |
| Factor   | `factor()` | `id \| num \| ( Expr )` |

#### 3.3.3 运算符优先级实现

优先级通过**函数调用层级**隐式实现——`expression()` 先调用 `term()`，`term()` 先调用 `factor()`：

```
expression()   →  处理 + - （最低优先级）
    └── term() →  处理 * / （中等优先级）
        └── factor() →  处理 id/num/(expr) （最高优先级）
```

所以对于 `a + b * c`，`term()` 会先把 `b * c` 结合，再回到 `expression()` 做 `a + 结果`。

#### 3.3.4 中间代码生成

在解析过程中同步生成四元式：
```
a = 5 * 2 + 10

┌─────────────┬──────────────────────────────────────┐
│ 四元式      │ 含义                                  │
├─────────────┼──────────────────────────────────────┤
│ (*,5,2,t1)  │ t1 = 5 * 2                           │
│ (+,t1,10,t2)│ t2 = t1 + 10                         │
│ (=,t2,_,a)  │ a = t2                               │
│ (=,a,_,b)   │ b = a                                │
└─────────────┴──────────────────────────────────────┘
```

#### 3.3.5 语义检查：符号表

- **填入**：解析声明语句 `int a;` 时，将 `a` 加入符号表
- **查询**：解析赋值语句 `a = ...`时，检查 `a` 是否在符号表中
- **报错**：如果 `a` 不在符号表中 → 抛出 "变量未声明" 错误

这实现了"先声明后使用"的语义规则。

#### 3.3.6 答辩要点

1. **递归下降 vs 表驱动 LL(1)** —— 递归下降更直观，每个函数对应一个非终结符，代码即文档。但表驱动 LL(1) 更通用（改文法只需改表）。
2. **为什么使用 NodeValue 内部类？** —— 解析函数需要同时返回表达式的"值"（用于四元式）和"语法树节点"（用于可视化）。Java 单返回值限制，所以打包为内部类。
3. **回填技术** —— while/if 语句生成条件跳转指令时，跳转目标暂时未知，先填占位符 `0`。等循环体/if 体解析完毕后，再回填正确的地址。这是单遍扫描生成代码的经典技术。

---

### 阶段 4：中间代码优化

**在教材中的位置**：§9.1-§9.2（基本块优化、常量传播、常量合并）

#### 3.4.1 常量合并（Constant Folding）

**定义**：在编译时计算常量表达式的值，而非生成运行时的计算指令。

```
源代码: a = 5 * 2 + 10

优化前四元式:                      优化后四元式:
0: (*, 5, 2, t1)       →          0: (=, 10, _, t1)    ← 5*2 编译时算出 = 10
1: (+, t1, 10, t2)     →          1: (+, 10, 10, t2)   ← t1 被替换为 10
                                    (进一步触发合并)
                                  → 1: (=, 20, _, t2)   ← 10+10 编译时算出 = 20
2: (=, t2, _, a)       →          2: (=, 20, _, a)     ← 直接赋值 20
```

#### 3.4.2 常量传播（Constant Propagation）

**定义**：如果一个变量在某个程序点是常数值，则将该值替换到后续使用处。

```
t1 = 10          →  constMap 记录: {"t1" → "10"}
t2 = t1 + 5      →  arg1 从 "t1" 替换为 "10"，变成 t2 = 10 + 5
                 →  触发常量合并: t2 = 15
```

#### 3.4.3 算法流程

```
遍历每条四元式 q:
    ① 常量传播：检查 q.arg1 / q.arg2 是否在 constMap 中
       在 → 替换为常量值
       
    ② 常量合并：如果 arg1 和 arg2 都是数值常量
       编译时计算 op(arg1, arg2)
       成功 → 生成 (=, 结果, _, q.result)，记入 constMap
       失败 → 保留原指令
       
    ③ 常量赋值记录：如果 q 是 (=, 常量, _, tempVar)
       将 tempVar → 常量 记入 constMap
       
    ④ 其它指令：保留原样
```

#### 3.4.4 答辩要点

1. **为什么不优化 `b = a`？** —— `a` 的值在编译时未知（虽然在这个简单例子中 a=20，但 `a` 不是临时变量 t1/t2，我没有追踪用户变量的值）。跨语句的值追踪在实际编译器中需要数据流分析。
2. **优化的安全性** —— 常量合并保证不会改变程序语义，因为常量表达式的值在运行时和编译时完全一致（除浮点数可能有点差异，整数运算是精确等价的）。
3. **干净代码（cleanUp）的预留** —— 当前 cleanUp 只是返回原序列，但在实际编译器中应该删除从未被使用的临时变量赋值（死代码消除）。

---

### 阶段 5：可视化报告

#### 3.5.1 5 个选项卡

| 选项卡 | 展示内容 | 对应阶段 |
|--------|----------|----------|
| 1. 词法分析 | Token 序列表格（序号/内容/类型/位置） | 阶段 1 |
| 2. LL(1) 理论 | FIRST/FOLLOW 集合 + 预测分析表 M | 阶段 2 |
| 3. 语法树 | CSS 树形结构可视化 | 阶段 3 |
| 4. 代码优化 | 优化前后左右对比（diff 视图） | 阶段 4 |
| 5. 最终中间代码 | 优化后的四元式序列 | 阶段 4 |

#### 3.5.2 技术亮点

- 纯前端实现（HTML + CSS + JS），无需服务器，浏览器直接打开
- CSS 伪元素绘制语法树连接线（无需 Canvas/SVG）
- 选项卡切换用原生 JavaScript（零框架依赖）
- 蓝色卡片式布局（Ant Design 风格）

---

## 四、关键数据结构

### 4.1 Token（二元式）

```
┌─────────────────────────────────┐
│            Token                 │
├─────────────────────────────────┤
│ - type: TokenType  // 种别码    │
│ - value: String    // 属性值    │
│ - line: int        // 行号      │
│ - column: int      // 列号      │
└─────────────────────────────────┘
```

### 4.2 Quadruple（四元式）

```
┌──────────────────────────────────────┐
│            Quadruple                 │
├──────────────────────────────────────┤
│ - op: String     // 运算符           │
│ - arg1: String   // 第一操作数       │
│ - arg2: String   // 第二操作数       │
│ - result: String // 结果/跳转目标    │
└──────────────────────────────────────┘

示例: ( + , t1 , 10 , t2 )
        ↑    ↑    ↑    ↑
       操作 左操作数 右操作数 结果
```

### 4.3 TreeNode（语法树节点）

```
        Program
        ├── int
        ├── main
        ├── (
        ├── )
        ├── {
        ├── StmtList
        │   ├── Statement
        │   │   └── Decl
        │   │       ├── int
        │   │       ├── id: a
        │   │       └── ;
        │   └── Statement
        │       └── Assign
        │           ├── id: a
        │           ├── =
        │           ├── Expr(+)
        │           │   ├── Expr(*)
        │           │   │   ├── 5
        │           │   │   └── 2
        │           │   └── 10
        │           └── ;
        └── }
```

### 4.4 预测分析表 M

```
          int         id          +          ;          }          #
Program   P→int...
StmtList  S→S SL                  S→ε                  S→ε
Stmt      S→Decl      S→Assign
Decl      D→int id;
Assign                A→id=Expr;
Expr                  E→T EP
ExprP                             E→+T EP   E→ε
Term                  T→id
```

---

## 五、测试用例与运行结果

### 5.1 测试源代码

```c
int main() {
    int a;
    int b;
    a = 5 * 2 + 10;   // 可被常量合并优化
    b = a;
}
```

### 5.2 控制台输出（关键部分）

**词法分析 → 全部 Token 被正确识别**

**LL(1) 预测分析表 → 全部表项非冲突（证明是 LL(1) 文法）**

**原始四元式**：
```
0: (*   , 5   , 2   , t1  )    // t1 = 5 * 2
1: (+   , t1  , 10  , t2  )    // t2 = t1 + 10
2: (=   , t2  , _   , a   )    // a = t2
3: (=   , a   , _   , b   )    // b = a
```

**优化后四元式**：
```
0: (=   , 10  , _   , t1  )    // 5*2 编译时算出 = 10
1: (=   , 20  , _   , t2  )    // 10+10 = 20（经过传播+合并）
2: (=   , 20  , _   , a   )    // a 直接获得 20
3: (=   , a   , _   , b   )    // b = a（跨变量引用，不做传播）
```

### 5.3 优化效果

```
优化前：4 条指令，2 次运行时计算
优化后：4 条指令，0 次运行时计算（5*2 和 10+10 都在编译时完成）
```

---

## 六、答辩可能问到的问题（Q&A）

### Q1：你的词法分析器是如何处理错误的？

**A**：遇到非法字符时，不中断分析，而是：
1. 将该字符标记为 `UNKNOWN` 种别码
2. 记录错误信息（含行列号）
3. 跳过该字符继续分析

这叫做**错误恢复**（应急模式），能在一次分析中报告所有词法错误而非遇错即停。

### Q2：为什么 LL(1) 分析和实际语法分析用了不同的方法？

**A**：项目中 LL1Analyzer 用于**理论展示**（计算 FIRST/FOLLOW/预测分析表），Parser 用于**实际解析**。实际解析用了递归下降法，它等价于 LL(1) 预测分析，但代码更直观。两者基于同一个 LL(1) 文法。

### Q3：你的文法真的是 LL(1) 吗？怎么验证？

**A**：是的。验证标准：预测分析表中每个格子最多只有一条产生式。我们的程序的输出表确实每个格子要么为空要么只有一条产生式，没有冲突。

### Q4：运算符优先级是怎么实现的？

**A**：通过递归调用的层级结构隐式实现。高层函数 `expression()` 处理低优先级（+、-），它调用中层函数 `term()` 处理高优先级（*、/），`term()` 调用底层函数 `factor()` 处理最小单元。这种结构自然保证了 `*` 先于 `+` 计算。

### Q5：你的优化器能做哪些优化？有哪些局限？

**A**：
- **能做**：常量合并（编译时计算常量表达式）、常量传播（将常数值替换到使用处）
- **局限**：不跨控制流、不处理用户变量传播、不做死代码消除、不做公共子表达式消除。这些都是基本块内的简单优化。

### Q6：为什么要生成四元式而不是直接生成目标代码？

**A**：四元式是**机器无关**的中间表示。它作为前端和后端的桥梁：
1. 与目标机器无关，便于跨平台
2. 便于进行机器无关的优化（如常量合并）
3. 若有需要，可以轻松扩展为生成 x86、ARM 或 JVM 字节码

### Q7：符号表在你的编译器中起什么作用？

**A**：符号表实现**语义检查**中的"先声明后使用"规则。在解析声明语句时将变量名加入符号表，在解析赋值语句时检查变量是否已声明。如果使用了未声明变量，立即报语法错误。

### Q8：如果"5 * 2"改成"5 / 0"，你的优化器会怎么处理？

**A**：`calculate()` 方法会检测到除数为 0，返回 `null`（表示无法优化）。此时保留原始的四元式 `(/ , 5, 0, t1)`，让运行时去处理除零错误。这样保证了优化的**安全性**。

### Q9：你用了什么设计模式？

**A**：
- **流水线模式**：Main 类中五个阶段串联执行
- **访问者模式的变体**：递归下降分析本质是遍历语法树的过程
- **DTO 模式**：Token、Quadruple 等是纯数据对象
- **建造者模式**：StringBuilder 用于累积字符串拼接

### Q10：如果需要扩展支持更多语法（如数组、函数调用），应该怎么改？

**A**：
1. 词法层面：在 TokenType 中可能不需要改动（括号就是界符，逗号就是界符）
2. 文法层面：添加新的产生式，如 `Factor → id [Expr]`（数组访问）
3. 解析层面：添加新的解析函数
4. 中间代码：添加新的四元式操作码（如 `[]=` 数组赋值）
5. LL1Analyzer 中重新计算 FIRST/FOLLOW/预测分析表

---

## 补充 Q&A：词法分析深入

### Q11：你的词法分析器有缓冲区吗？怎么处理回退（rollback）？

**A**：本项目没有显式的输入缓冲区，而是直接操作 Java 的 String（底层是 char 数组）。通过 `pos` 指针控制扫描位置，通过 `peek()` 实现不消耗字符的前瞻。如果前瞻后发现不需要消耗（如单字符运算符的情况），只需不调用 `advance()` 即可，本质上就是一次 rollback。实际编译器（如 GCC/Clang）使用双缓冲区（double buffering）来高效处理大文件的 I/O。

### Q12：NFA 和 DFA 的关系是什么？为什么你选择手写 DFA 而不是先用正则转 NFA 再转 DFA？

**A**：NFA（不确定有限自动机）对同一字符可以从一个状态转移到多个状态，而 DFA（确定有限自动机）每个状态对每个字符最多只有一个转移。理论上正则→NFA→DFA→最小化 DFA 是全自动化的，但会生成大量中间状态。手写 DFA 更简洁高效（9 个状态足够），而且可以在各状态内部融入灵活的语义动作（如错误恢复、转义处理），这是全自动词法生成器（如 Lex/Flex）难以简洁表达的。

### Q13：为什么关键字用 HashSet 存而不是直接写死在状态转换里？

**A**：分离关注点。状态转换负责"识别标识符的形式"（字母开头，字母/数字/下划线组成），关键字集合负责"判定标识符是否为保留字"。如果把每个关键字写进 DFA 状态（例如"读了 i 后进入 in_state，读 n 进入 int_state...")，会导致状态爆炸（15 个关键字意味着几十个额外状态）。用 HashSet 查找更简洁，且添加新关键字只需一行代码。

### Q14：你的词法器能正确区分 `12.` 和 `12.5` 吗？为什么这样设计？

**A**：能。识别数字时，遇到小数点后会再前瞻一个字符——如果小数点后是数字，识别为浮点数（`12.5` → FLOAT）；如果小数点后不是数字，则小数点不消耗，整数部分 `12` 返回为 INTEGER，小数点留待后续识别为 DELIMITER。这符合 C/Java 的语法：`12.` 中 `.` 是成员访问运算符，不是浮点数的一部分。

### Q15：字符串里的转义序列 `\"` 你是怎样避免和字符串结束符冲突的？

**A**：识别字符串时，读到反斜杠 `\` 就立即进入转义处理模式——消耗反斜杠和下一个字符，把它们作为整体处理。如果下一个字符是 `"`，不会触发"遇到闭合引号"的分支，而是作为普通字符追加到结果中。这个逻辑的优先级高于字符串结束判定。

---

## 补充 Q&A：语法分析深入

### Q16：如果一个文法有左递归，你能具体演示一下递归下降分析为什么会死循环吗？

**A**：假设有左递归文法 `Expr → Expr + Term | Term`。对应的递归下降函数如果这样写：
```
void expr() {
    expr();     // ← 第一步就递归调用自己！
    match('+');
    term();
}
```
第一次调用 `expr()` 时，还没消耗任何 Token 就立刻再次调用 `expr()`，导致无限递归、栈溢出。消除左递归后：`Expr → Term ExprP`，`expr()` 先调用 `term()`（会消耗 Token），再调用 `exprP()`，每次递归前都有 Token 被消耗，保证了终止。

### Q17：你的文法支持 else 悬空问题吗？如果支持，你是怎么解决 else 匹配的？

**A**：本项目目前的文法没有 else 子句（if 只有 if-true 体），所以不存在 else 悬空问题。如果扩展支持 `if-else`，则需要在文法中体现"每个 else 与最近的未匹配 if 结合"的规则，通常的 LL(1) 改写方式是将 Stmt 中的 If 拆分为 MatchedStmt（含匹配的 if-else）和 UnmatchedStmt（if 不含 else），以此消除二义性。

### Q18：为什么你的 if 和 while 语句没有出现在 LL1Analyzer 的文法中，但 Parser 中却实现了解析？

**A**：这是一个好问题。LL1Analyzer 中定义的是简化版文法（用于演示 FIRST/FOLLOW/预测分析表计算），Parser 中实现的是更完整的文法（包含 if/while）。这是因为 LL1Analyzer 的核心目的是展示理论推导过程，文法越简洁越容易理解；而 Parser 是实际执行解析的程序，功能越完整越好。两者的核心（递归下降 + LL(1) 决策）是一致的，只是产生式数量不同。在实际答辩中，可以说明这一点：**理论分析用简化文法展示方法论，工程实现用完整文法展示扩展性**。

### Q19：你的语法分析器能报出多个语法错误吗？还是遇错即停？

**A**：当前实现是遇错即停（遇到第一个语法错误就抛出 RuntimeException）。完整的编译器应该支持错误恢复——例如在遇到语法错误后，跳过一些 Token（跳到下一个分号或 } 等同步符号），然后继续分析后面的内容，这样可以在一次编译中报告所有语法错误。这是我后续可以改进的地方。

### Q20：递归下降分析中，如何处理 ε-产生式（空产生式）？

**A**：ε-产生式意味着"什么都不匹配"。在递归下降分析中，ε-产生式对应一个不做任何匹配操作的分支。例如 `statementList()` 函数中，while 循环的条件判断 `!currentToken.getValue().equals("}")` ——当遇到 `}` 时，不消耗任何 Token，直接返回空节点。这就是 ε-产生式 `StmtList → ε` 的选择条件：lookahead 符号属于 FOLLOW(StmtList) = {}}。

---

## 补充 Q&A：语义分析与中间代码

### Q21：四元式和三元式、间接三元式有什么区别？你为什么选四元式？

**A**：

| 表示形式 | 结构 | 优点 | 缺点 |
|----------|------|------|------|
| 三元式 | (op, arg1, arg2) | 不需要临时变量名 | 移动代码时需修改所有引用 |
| 间接三元式 | 三元式 + 间接索引表 | 代码移动方便 | 多一层间接性 |
| 四元式 | (op, arg1, arg2, result) | 结果有显式名称，便于优化和寄存器分配 | 需要管理临时变量名 |

选择四元式是因为它的 result 字段使得优化器可以直接通过名称追踪值的流动（如常量传播时 constMap["t1"] → "10"），且与传统教材一致。

### Q22：你的临时变量 t1, t2, t3... 是无限的，实际编译器如何处理无限临时变量？

**A**：实际编译器通过**寄存器分配**将无限的临时变量映射到有限的寄存器（或内存）。核心算法是图着色法：构建寄存器冲突图（两个临时变量同时活跃则冲突），然后用不超过寄存器数量的颜色给图着色。无法着色的变量溢出到内存（spill）。本项目的临时变量仅用于中间表示，编译器后端才需要寄存器分配，不在本项目的实现范围内。

### Q23：回填（Backpatching）的具体过程是怎样的？你怎么知道跳转地址应该填什么？

**A**：回填的核心是"先生成跳转指令 + 占位符，确定目标地址后回来修改"。具体做法：
1. 生成条件跳转 `j> a, b, 0` 时，记录这个四元式的**索引** `jumpAddr`（如索引 3），目标地址暂填 `0`
2. 继续解析后续代码，生成更多四元式
3. 当出口地址确定时（即当前 quads.size() 的值，如 8），通过 `quads.get(jumpAddr).result = "8"` 回填

因为四元式按顺序存储在 ArrayList 中，其索引天然就是地址。我们只需要记住"哪条指令需要回填"（通过保存其索引），再在合适的时机"填上目标索引"即可。

### Q24：你的符号表如果遇到变量重复声明会怎样？

**A**：当前实现中 `symbolTable.add(name)` 使用 HashSet，重复声明不会报错（Set 自动去重）。完整的编译器应该在 add() 时检查是否已存在，若存在则报"变量重复定义"错误。这是符号表可以改进的一个点，涉及符号表需要增加 `insert()` 方法（定义时检查）和 `lookup()` 方法（使用时检查）的区分。

---

## 补充 Q&A：优化与工程实践

### Q25：常量传播中，如果遇到 `t1 = t1 + 1` 这样的自增操作，你的优化器会怎么处理？

**A**：当前优化器不会处理这种情况。因为 `t1 = t1 + 1` 涉及对同一变量的读写——右侧的 t1 在 constMap 中为 "10"，左侧的 t1 如果用 constMap 中取出的值替换，得到 `t1 = 10 + 1 = 11`。但关键问题是：**赋值后 t1 的值变了**，constMap 中原来的 "t1→10" 应该更新为 "t1→11"。当前优化器的 constMap 不会在赋值后删除旧值，所以如果后面再使用 t1，得到的是旧值 10 而非新值 11。这说明常量传播需要更精细的**到达-定值分析**（Reaching Definitions），确保只传播在当前位置确实有效的常量值。

### Q26：你的优化和 GCC 的 -O0, -O1, -O2, -O3 优化级别相比，处于什么水平？

**A**：
- **GCC -O0**：不做优化，仅做最简单的代码生成
- **GCC -O1**：基础优化，包括常量合并、死代码消除、基本块合并等
- **GCC -O2**：更多优化，包括公共子表达式消除、指令调度等
- **GCC -O3**：激进优化，包括函数内联、循环向量化等

本项目的优化大致相当于 **-O1 的一小部分**——只做了常量合并和常量传播，缺少死代码消除、公共子表达式消除、循环优化等。但从教学角度看，已经展示了编译器优化的核心思想。

### Q27：如果让你重新设计这个项目的架构，你会做什么改变？

**A**：
1. **解耦语法分析与代码生成**：当前 Parser 同时做解析、语义检查、四元式生成，职责过重。应该先解析生成完整的 AST，然后分别做语义遍历、中间代码生成遍历，每个遍历独立
2. **引入 Visitor 模式**：对 AST 的每个操作（类型检查、代码生成、优化）都实现为独立的 Visitor，便于扩展
3. **错误管理器**：集中管理错误信息（ErrHandler），支持多错误收集、按行号排序输出
4. **配置文件**：将文法定义外部化（如 JSON/YAML），避免硬编码在代码中
5. **单元测试**：为词法分析、语法分析的每个规则编写测试用例

### Q28：你是如何测试你的编译器正确性的？

**A**：主要用了两种方法：
1. **手工构造测试用例**：设计了含变量声明、算术表达式（含优先级验证）、赋值传播、常量表达式的综合性源程序，覆盖编译器的各个阶段
2. **输出交叉验证**：将词法分析的 Token 流、语法分析的语法树、四元式序列与手工推导的结果进行对比
3. **优化前后对比**：验证优化后的四元式序列是否语义等价于优化前

在实际工程中，应该补充单元测试（JUnit）和回归测试，但我当前没有做到。

### Q29：如果你的编译器和 GCC/Clang 编译同一个程序，产出的中间代码会有什么差别？

**A**：差别会非常大：
1. **IR 的复杂度**：GCC 的 GIMPLE 和 Clang 的 LLVM IR 都是 SSA 形式（静态单赋值），每个变量只被赋值一次，有利于数据流分析。本项目的四元式是非 SSA 的。
2. **类型系统**：GCC/Clang 的 IR 保留了完整的类型信息，本项目的四元式所有操作数都是字符串，没有类型标注。
3. **优化广度**：GCC 有超过 200 种优化 pass，本项目只有 2 种。
4. **规模**：即使编译一个简单的 `hello world`，GCC 产出的 IR 也可能有几百条指令（包含库函数调用、初始化代码等），本项目的 IR 非常简洁。

但这些差异不影响在课程范围内展示编译器核心原理。

### Q30：JVM 字节码也是一种中间表示，你能对比一下四元式和 JVM 字节码吗？

**A**：

| 维度 | 四元式 | JVM 字节码 |
|------|--------|------------|
| 形式 | 三地址码（op, a1, a2, r） | 基于栈的指令（iload, iadd, istore） |
| 可读性 | 人类友好 | 需要学习指令集 |
| 操作数 | 显式命名（t1, a, 10） | 隐式（栈顶操作） |
| 可优化性 | 容易做数据流分析 | 需要重建表达式树 |
| 平台相关性 | 完全无关 | 针对 JVM 设计 |

举个例子：`a = 5 + 3 * 2` 对应的表示：
- 四元式：`(*,3,2,t1)` `(+,5,t1,t2)` `(=,t2,_,a)`
- JVM 字节码：`bipush 3; bipush 2; imul; bipush 5; iadd; istore_1`

四元式更适合优化分析，字节码更适合执行。

---

### Q31：你提到了"不动点迭代"，能解释一下为什么 FIRST/FOLLOW 算法一定会终止？

**A**：因为三个因素保证了终止性：
1. **有限性**：文法的终结符和非终结符数量都是有限的，因此每个 FIRST 集 / FOLLOW 集的基数有上限
2. **单调递增**：每轮迭代中，集合的大小只增不减（我们只做 `add` 和 `addAll` 操作）
3. **有上界**：FIRST 集最多包含所有终结符 + ε，FOLLOW 集最多包含所有终结符 + #

一个有上界的单调递增序列必定收敛（不动点定理）。所以算法一定会在有限步内终止。实际测试中，本项目的文法通常在 3-5 轮迭代内收敛。

### Q32：你的词法分析器用 Java 实现，如果源程序很大（几万行），会不会有效率问题？

**A**：会有一定影响。Java String 是 final 不可变对象，每次 `sb.append()` 和 `sb.toString()` 都会产生新的字符串，对于大文件造成 GC 压力。但实际编译器中大部分时间花在语法分析和优化上，词法分析的时间占比通常不大。另外 JVM 的 JIT 编译器会对热点代码做优化，所以对于课程项目的规模完全够用。生产级词法分析器（如 GCC 的 lexer）会用 C 语言、手动管理内存、使用 mmap 直接映射文件到内存。

### Q33：你的 LL1Analyzer 的 computeFirst() 只取右部第一个符号，这是正确的吗？

**A**：对于本项目定义的文法，右部所有产生式的首符号要么是终结符、要么是 ε、要么是其 FIRST 集不含 ε 的非终结符。因此只取第一个符号是正确的。但如果存在非终结符 X 使得 ε ∈ FIRST(X)，则需要级联处理：若 X → ε，且产生式是 A → X Y Z，则需要将 FIRST(Y) 也加入 FIRST(A)。这是完整 FIRST 算法需要的，本项目的文法恰好没有这种 ε 级联的情况，所以简化实现也能正确工作。

### Q34：你在实现过程中遇到的最大困难是什么？怎么解决的？

**A**：最大的困难是**表达式文法的左递归消除**。最初的表达式文法写成 `Expr → Expr + Term | Term`，递归下降函数直接调用自己导致栈溢出。解决方法是按照教材将文法改写为：
```
Expr  → Term ExprP
ExprP → + Term ExprP | ε
```
这是标准的消除左递归技术。但改写后的文法使得**语法树的形状**不够自然（`a + b + c` 会生成右结合的树而非左结合），所以我在构建 TreeNode 时做了特殊处理，将 `Expr(+ op)` 作为内部节点，保证最终的语法树左子树的形状在可视化报告上易于理解。

### Q35：你的项目文件里有一个 `compiler_report.html`，这个文件的作用是什么？什么时候生成的？

**A**：这是 WebVisualizer 在程序运行结束时自动生成的可视化报告。它在编译的**所有阶段完成之后**生成，汇总了词法分析、LL(1) 理论、语法树、优化前后的所有数据，渲染为一个交互式 HTML 网页。双击用浏览器打开即可查看，有 5 个可切换的选项卡。生成的时机在 Main.java 的第 5 阶段，调用 `WebVisualizer.generateReport(tokens, analyzer, root, rawQuads, optimizedQuads)`。

---

## 七、总结与收获

### 7.1 理论到实践的映射

| 教材概念 | 代码实现 |
|----------|----------|
| DFA（确定有限自动机） | Lexer 的状态转换枚举 + tokenize() 主循环 |
| 二元式（Token） | Token 类 |
| LL(1) 文法 | addProduction() 定义的产生式集合 |
| FIRST/FOLLOW 集合 | LL1Analyzer 的不动点迭代算法 |
| 预测分析表 M | parsingTable（Map 的 Map） |
| 递归下降分析 | Parser 中每个非终结符对应一个函数 |
| 四元式 | Quadruple 类 |
| 符号表 | SymbolTable 类 |
| 常量合并/传播 | Optimizer 的单遍扫描算法 |

### 7.2 项目亮点

1. **完整的全流程**：从字符到优化后的中间代码，编译器前端的每个阶段都有实现
2. **理论与实践结合**：LL1Analyzer 展示了理论计算，Parser 展示了实际解析
3. **可视化报告**：HTML 报告让答辩时有直观的展示材料
4. **代码规范**：Javadoc 注释完整，每个方法都有算法说明
5. **零依赖**：只用 JDK 标准库，不依赖任何第三方框架

### 7.3 答辩演示建议

1. **开场**（1 分钟）：介绍项目目标——实现一个完整的编译器前端
2. **架构**（1 分钟）：展示流水线图，说明 5 个阶段的输入输出
3. **词法分析**（1 分钟）：展示 Token 序列表格，解释 DFA 状态转换
4. **LL(1) 理论**（1 分钟）：展示预测分析表，解释 FIRST/FOLLOW 含义
5. **语法分析**（1 分钟）：展示语法树 + 原始四元式
6. **代码优化**（1 分钟）：**重点展示**优化前 5*2+10 → 优化后 20 的过程
7. **总结**（30 秒）：强调收获和亮点
