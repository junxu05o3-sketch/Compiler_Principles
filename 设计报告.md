# 简单编译器的设计与部分实现

## 一、设计目标

本设计目标是实现一个**简易 C/Java 风格语言编译器**的前端部分，包括词法分析、语法分析、语义分析、中间代码生成和代码优化。

编译器能够处理的源语言子集包括：变量声明（int 类型）、赋值语句（含算术表达式和关系表达式）、if 条件语句、while 循环语句。表达式支持加减乘除四则运算、括号嵌套，以及 >、<、>=、<=、==、!= 六种关系运算符。运算符优先级遵循 C/Java 语言规范（乘除优先于加减）。

最终输出包括：Token 序列（二元式）、LL(1) 预测分析表、语法树、四元式中间代码、优化后的四元式，以及 HTML 可视化报告。

## 二、实验要求

根据编译器的设计五个阶段要求，按词法扫描器、语法分析器、中间代码生成器、优化器、目标代码生成器分别加以设计说明、符号表设计、报错处理等。本次设计主要要求完成：

1. **词法分析器功能模块**（必做）
2. **语法分析器功能模块**（必做）
3. **中间代码生成器模块**（选做，已实现）
4. **优化模块**（选做，已实现常量合并与常量传播）
5. **目标代码生成模块**（选做，未实现，报告中给出设计思路）

## 三、词法分析器的设计方法

### 词法分析器的基本原理

词法分析是编译的第一个阶段，其任务是将源程序字符串中的字符序列转换为有意义的单词（Token）序列。这一过程本质上是在模拟一个确定有限自动机（DFA）。本设计采用手写 DFA（状态转换图）方法实现词法分析器，核心思路为：

1. 将识别每种单词的过程抽象为一个"状态"
2. 从初始状态 START 出发，根据当前读入的字符决定进入哪个子状态
3. 子状态持续读入字符，直到遇到不属于该单词的字符为止
4. 依据最长匹配原则，识别出一个完整单词并生成二元式 Token

#### 3.1 基本符号表的设计

对单词符号进行种别码分类。标识符和整数常量一类一码，关键字和界符都是一符一码，详细的单词符号种别码表如下。

| 单词符号 | 种别码 | 单词符号 | 种别码 |
|----------|:------:|----------|:------:|
| 标识符 | IDENTIFIER | int | KEYWORD |
| main | KEYWORD | if | KEYWORD |
| while | KEYWORD | return | KEYWORD |
| float | KEYWORD | double | KEYWORD |
| char | KEYWORD | void | KEYWORD |
| bool | KEYWORD | else | KEYWORD |
| for | KEYWORD | break | KEYWORD |
| continue | KEYWORD | true/false/null | KEYWORD |
| 整数常量 | INTEGER | 浮点数常量 | FLOAT |
| 字符串常量 | STRING | 字符常量 | CHAR |
| != | OPERATOR | >= | OPERATOR |
| <= | OPERATOR | == | OPERATOR |
| = | OPERATOR | > | OPERATOR |
| < | OPERATOR | + | OPERATOR |
| - | OPERATOR | * | OPERATOR |
| / | OPERATOR | ! | OPERATOR |
| && | OPERATOR | \|\| | OPERATOR |
| ++ | OPERATOR | -- | OPERATOR |
| += | OPERATOR | -= | OPERATOR |
| *= | OPERATOR | /= | OPERATOR |
| { | DELIMITER | } | DELIMITER |
| ( | DELIMITER | ) | DELIMITER |
| [ | DELIMITER | ] | DELIMITER |
| ; | DELIMITER | , | DELIMITER |
| 文件结束符 | EOF | 非法字符 | UNKNOWN |

本设计采用 Java 枚举（enum）定义种别码，共 10 种类型：KEYWORD（关键字）、IDENTIFIER（标识符）、INTEGER（整数）、FLOAT（浮点数）、STRING（字符串）、CHAR（字符）、OPERATOR（运算符）、DELIMITER（界符）、EOF（文件结束）、UNKNOWN（非法字符）。

文法产生式在第四节中给出。

#### 3.2 状态转换图描述

词法分析器内部维护了 9 个状态，模拟 DFA 的状态转换：

```
                     ┌──────────┐
               ┌────→│  START   │←────┐
               │     └────┬─────┘     │
         字母/_│    数字  │  "  '  运算符  界符  /+字母
               ↓     ↓    ↓  ↓    ↓     ↓     ↓
          IN_ID  IN_INT  IN_STRING IN_CHAR IN_OP DELIMITER→直接输出
            │      │                              │
            │  遇到'.'?                           │
            │      ↓                              ↓
            │  IN_FLOAT                      判断是否为注释
            ↓      ↓                              │
          (返回Token)                  //→LINE_COMMENT
                                       /*→BLOCK_COMMENT
```

各状态含义：

- **START**：初始/空白跳过状态，每个新 Token 识别的起点
- **IN_ID**：正在识别标识符或关键字，以字母/下划线开头，持续读取字母/数字/下划线
- **IN_INT**：正在识别整数，持续读取数字序列
- **IN_FLOAT**：正在识别浮点数，已读入整数部分和小数点后读取小数部分
- **IN_STRING**：正在识别字符串常量，读到闭合双引号结束
- **IN_CHAR**：正在识别字符常量，读到闭合单引号结束
- **IN_OP**：正在识别运算符，可能需要前瞻一个字符判断是否为双字符运算符
- **IN_LINE_COMMENT**：正在跳过单行注释（//），读到换行符结束
- **IN_BLOCK_COMMENT**：正在跳过多行块注释（/\* ... \*/），读到 \*/ 结束

#### 3.3 词法分析算法设计与代码实现

词法分析器（Lexer 类）的核心方法为 `tokenize()`，其算法流程如下：

1. **跳过空白**：忽略空格、制表符、换行符等空白字符
2. **判断首字符类型**：根据首字符决定进入哪个识别子程序
3. **循环识别**：重复步骤 1-2 直到源程序全部扫描完毕
4. **追加 EOF**：在 Token 序列末尾添加文件结束标记

**标识符/关键字识别算法**：

```
while (当前字符是字母/数字/下划线) {
    读入字符
}
if (读到的字符串在关键字集合中)
    → 种别码为 KEYWORD
else
    → 种别码为 IDENTIFIER
```

采用最长匹配原则（Maximal Munch）：当输入"ifa"时，虽然"if"是关键字，但由于后面跟了'a'且'a'是合法标识符字符，所以整体识别为标识符"ifa"。

**双字符运算符前瞻算法**：

```
读取第一个运算符字符 c
if (c + 下一个字符 构成合法的双字符运算符)  // 如"=="、"!="、"<="、">="、"&&"、"||"
    消耗第二个字符，生成双字符运算符 Token
else
    生成单字符运算符 Token
```

支持的双字符运算符共 12 种：==、!=、<=、>=、&&、||、++、--、+=、-=、*=、/=。

**注释跳过处理**：注释在词法阶段直接忽略，不产生任何 Token。单行注释（//）跳到行尾的换行符前；块注释（/\* \*/）跳到闭合标记处。若块注释未闭合（到达文件末尾仍未找到 \*/），记录错误信息。

**核心代码实现**（`tokenize()` 方法主循环）：

```java
public List<Token> tokenize() {
    while (pos < source.length()) {
        skipWhitespace();  // 跳过空白字符
        if (pos >= source.length()) break;
        char c = peek();
        
        // 注释处理（优先级最高）
        if (c == '/' && pos + 1 < source.length()) {
            char next = source.charAt(pos + 1);
            if (next == '/') { skipLineComment(); continue; }
            else if (next == '*') { skipBlockComment(); continue; }
        }
        
        // 标识符/关键字
        if (Character.isLetter(c) || c == '_') 
            { tokens.add(readIdentifierOrKeyword()); continue; }
        // 数字常量
        if (Character.isDigit(c)) 
            { tokens.add(readNumber()); continue; }
        // 字符串常量
        if (c == '"') { tokens.add(readString()); continue; }
        // 字符常量
        if (c == '\'') { tokens.add(readChar()); continue; }
        // 运算符
        if (isOperatorStart(c)) { tokens.add(readOperator()); continue; }
        // 界符
        if (isDelimiter(c)) { 
            tokens.add(new Token(TokenType.DELIMITER, String.valueOf(c), line, column));
            advance(); continue; 
        }
        // 非法字符（错误恢复）
        errors.add("非法字符 '" + c + "' 在 " + line + ":" + column);
        tokens.add(new Token(TokenType.UNKNOWN, String.valueOf(c), line, column));
        advance();
    }
    tokens.add(new Token(TokenType.EOF, "EOF", line, column));
    return tokens;
}
```

**Tokenizer 输出示例**（以测试源码 `int main() { int a; a = 5 * 2 + 10; }` 为输入）：

```
token: "int"          type: KEYWORD       [1:1]
token: "main"         type: IDENTIFIER    [1:5]
token: "("            type: DELIMITER     [1:9]
token: ")"            type: DELIMITER     [1:10]
token: "{"            type: DELIMITER     [1:12]
token: "int"          type: KEYWORD       [2:5]
token: "a"            type: IDENTIFIER    [2:9]
token: ";"            type: DELIMITER     [2:10]
token: "a"            type: IDENTIFIER    [4:5]
token: "="            type: OPERATOR      [4:7]
token: "5"            type: INTEGER       [4:9]
token: "*"            type: OPERATOR      [4:11]
token: "2"            type: INTEGER       [4:13]
token: "+"            type: OPERATOR      [4:15]
token: "10"           type: INTEGER       [4:17]
token: ";"            type: DELIMITER     [4:19]
token: "}"            type: DELIMITER     [5:1]
token: "EOF"          type: EOF           [5:2]
```

可以看到二元式的结果为（种别码, 单词符号）以及行列号。Token 序列被存储在一个有序列表（ArrayList）中，源程序中先出现的字符先存储，这样在后续语法语义分析中能够方便地按序访问。至此词法分析完成，为后续阶段提供了完整的 Token 流。

## 四、语法语义分析器的设计和中间代码生成

### 语法分析器的基本原理

语法分析阶段的主要任务是检查源代码是否符合文法要求。分析方法分为自底向上分析法（从源代码规约回文法开始符）和自顶向下分析法（从文法开始符正向推导到源代码）两大类。本次设计采用自顶向下的 LL(1) 文法进行分析。

LL(1) 的含义为：从左到右扫描输入（Left-to-right）、最左推导（Leftmost derivation）、向前看一个输入符号（1 symbol lookahead）。判断一个文法是否为 LL(1) 文法需要构造 First 集合和 Follow 集合。

#### 4.1 文法产生式集的设计

本设计定义的文法产生式如下：

```
1.  Program  → int main ( ) { StmtList }
2.  StmtList → Stmt StmtList
3.  StmtList → ε
4.  Stmt     → Decl
5.  Stmt     → Assign
6.  Stmt     → If
7.  Stmt     → While
8.  Decl     → int id ;
9.  Assign   → id = Expr ;
10. If       → if ( Expr relop Expr ) { StmtList }
11. While    → while ( Expr relop Expr ) { StmtList }
12. Expr     → Term ExprP
13. ExprP    → + Term ExprP
14. ExprP    → - Term ExprP
15. ExprP    → ε
16. Term     → Factor TermP
17. TermP    → * Factor TermP
18. TermP    → / Factor TermP
19. TermP    → ε
20. Factor   → id
21. Factor   → num
22. Factor   → ( Expr )
```

其中，产生式 12-22 利用引入非终结符 ExprP 和 TermP 的方法，消除了表达式文法中的左递归和公共左因子。

#### 4.2 转换为 LL(1) 文法

上述文法已经是 LL(1) 文法，满足两个条件：

1. 消除了左递归（如 Expr → Expr + Term 被改写为 Expr → Term ExprP，ExprP → + Term ExprP | ε）
2. 不存在公共左因子（每个非终结符的各个候选式的首终结符均不相同）

命名约定：以大写字母开头的符号为非终结符（如 Program、StmtList、Expr），以小写字母或特殊符号开头的为终结符（如 int、id、num、+、;），空串记为 ε（epsilon）。

#### 4.3 First集合构造实现

First 集合的定义为：从文法符号串 α 出发能推导出的所有终结符首符号的集合。如果 α 能推导出 ε（空串），则 ε ∈ First(α)。

实现采用**不动点迭代算法**：

1. 初始化：每个非终结符的 First 集为空集合
2. 重复遍历所有产生式，对每条产生式 A → X₁X₂...Xₙ：
   - 若 X₁ 是终结符或 ε → 将 X₁ 加入 First(A)
   - 若 X₁ 是非终结符 → 将 First(X₁) 全部合并到 First(A)
3. 直到没有新的符号加入任何 First 集为止（不动点收敛）

核心实现代码：

```java
public void computeFirst() {
    for (String nt : nonTerminals) firstSets.put(nt, new HashSet<>());
    boolean changed = true;
    while (changed) {
        changed = false;
        for (Production p : productions) {
            Set<String> firstLhs = firstSets.get(p.lhs);
            int beforeSize = firstLhs.size();
            String firstSymbol = p.rhs.get(0);
            if (firstSymbol.equals("ε") || terminals.contains(firstSymbol)) {
                firstLhs.add(firstSymbol);  // 规则1：首符号是终结符或ε
            } else {
                firstLhs.addAll(firstSets.get(firstSymbol));  // 规则2：非终结符→合并
            }
            if (firstLhs.size() > beforeSize) changed = true;
        }
    }
}
```

#### 4.4 Follow集合构造实现

Follow(A) 的定义为：在所有句型中，紧跟在非终结符 A 后面的终结符的集合。特别地，如果 A 是文法的开始符号或可以出现在句型最右端，则 #（输入结束符）∈ Follow(A)。

算法流程（按教材算法 4.2 实现）：

1. 初始化 Follow(S) = {#}（S 为开始符号），其余非终结符的 Follow 集为空
2. 反复扫描所有产生式，对每个非终结符 B：
   - 若产生式为 A → αBβ（B 后面还有符号 β）：
     - 若 β 是终结符 → 直接加入 Follow(B)
     - 若 β 是非终结符 → First(β) - {ε} 加入 Follow(B)
   - 若产生式为 A → αB（B 在末尾）→ Follow(A) 全部加入 Follow(B)
3. 直到所有 Follow 集不再扩大为止

核心实现代码：

```java
public void computeFollow(String startSymbol) {
    for (String nt : nonTerminals) followSets.put(nt, new HashSet<>());
    followSets.get(startSymbol).add("#");  // 开始符号加入结束符
    boolean changed = true;
    while (changed) {
        changed = false;
        for (Production p : productions) {
            for (int i = 0; i < p.rhs.size(); i++) {
                String symbol = p.rhs.get(i);
                if (nonTerminals.contains(symbol)) {
                    Set<String> followSymbol = followSets.get(symbol);
                    int beforeSize = followSymbol.size();
                    if (i + 1 < p.rhs.size()) {  // A → αBβ
                        String next = p.rhs.get(i + 1);
                        if (terminals.contains(next)) followSymbol.add(next);
                        else { Set<String> nextFirst = new HashSet<>(firstSets.get(next));
                               nextFirst.remove("ε"); followSymbol.addAll(nextFirst); }
                    } else {  // A → αB
                        followSymbol.addAll(followSets.get(p.lhs));
                    }
                    if (followSymbol.size() > beforeSize) changed = true;
                }
            }
        }
    }
}
```

**First/Follow 集合计算结果**（以本设计文法为输入）：

| 非终结符 | First 集合 | Follow 集合 |
|----------|-----------|-------------|
| Program | {int} | {#} |
| StmtList | {int, id, if, while, ε} | {}} |
| Stmt | {int, id, if, while} | {int, id, if, while, }} |
| Decl | {int} | {int, id, if, while, }} |
| Assign | {id} | {int, id, if, while, }} |
| Expr | {id, num, (} | {;, ), >, <, >=, <=, ==, !=} |
| ExprP | {+, -, ε} | {;, ), >, <, >=, <=, ==, !=} |
| Term | {id, num, (} | {;, ), +, -, >, <, >=, <=, ==, !=} |
| TermP | {\*, /, ε} | {;, ), +, -, >, <, >=, <=, ==, !=} |
| Factor | {id, num, (} | {;, ), +, -, \*, /, >, <, >=, <=, ==, !=} |

#### 4.5 预测分析表的创建

预测分析表 M 是一个二维表 M[A, a]，行为非终结符 A，列为终结符 a（含 #）。M[A, a] 的值是当栈顶为非终结符 A 且当前输入为 a 时应使用的产生式。

**构造算法**（按教材算法 4.3 实现）：

对每条产生式 A → α：
1. 确定该产生式的 lookahead 符号集合：
   - 若 α 首符号是 ε → lookahead = Follow(A)
   - 若 α 首符号是终结符 → lookahead = {首符号}
   - 若 α 首符号是非终结符 → lookahead = First(首符号)
2. 对每个 lookahead 中的终结符 a，将 A → α 填入 M[A, a]

如果某个表项 M[A, a] 为**空**，则表示遇到了语法错误（该非终结符不能以该终结符开头）。

验证文法是 LL(1) 文法：预测分析表中每个格子最多只有一条产生式，即不存在冲突。本设计的文法符合此条件。

**预测分析表 M（矩阵展示）**：

| NT \ T | int | id | num | + | - | * | / | ; | = | ( | ) | { | } | > < >= <= == != | # |
|--------|:---:|:---:|:---:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:---:|:-:|
| Program | P→int main()... | | | | | | | | | | | | | | |
| StmtList | S→St SL | S→St SL | | | | | | | | | | | S→ε | | |
| Stmt | S→Decl | S→Assign | | | | | | | | | | | | S→If/S→While | |
| Decl | D→int id; | | | | | | | | | | | | | | |
| Assign | | A→id=E; | | | | | | | | | | | | | |
| Expr | | E→T EP | E→T EP | | | | | | | E→T EP | | | | | |
| ExprP | | | | E→+T EP | E→-T EP | | | E→ε | | | E→ε | | E→ε | E→ε | |
| Term | | T→F TP | T→F TP | | | | | | | T→F TP | | | | | |
| TermP | | | | T→ε | T→ε | T→*F TP | T→/F TP | T→ε | | | T→ε | | T→ε | T→ε | |
| Factor | | F→id | F→num | | | | | | | F→(E) | | | | | |

从表中可以看到，每个格子最多只有一条产生式，无冲突，验证了文法确实是 LL(1) 文法。

#### 4.6 利用LL(1)分析以及语法树的生成

本设计采用**递归下降分析法**（Recursive Descent Parsing）实现语法分析。递归下降分析是 LL(1) 分析最直观的实现方式，其核心思想是为文法的每个非终结符编写一个解析函数。每个函数根据当前 lookahead 符号选择产生式，按产生式右部顺序依次调用相应的函数。

**解析函数与非终结符的对应关系**：

| 非终结符 | 解析函数 | 产生式 |
|----------|----------|--------|
| Program | parse() | int main () { StmtList } |
| StmtList | statementList() | Stmt StmtList \| ε |
| Stmt | statement() | Decl \| Assign \| If \| While |
| Decl | declaration() | int id ; |
| Assign | assignment() | id = Expr ; |
| Expr | expression() | Term { (+|-) Term } |
| Term | term() | Factor { (\*\|/) Factor } |
| Factor | factor() | id \| num \| ( Expr ) |

**运算符优先级实现**：通过函数调用层级隐式保证。expression() 处理 + 和 -（最低优先级），它调用 term() 处理 \* 和 /（次高优先级），term() 调用 factor() 处理基本单元（最高优先级）。使得对于 `a + b * c`，term() 会先将 `b * c` 结合，再回到 expression() 做 `a + 结果`。

**语法树的构建**：语法分析过程中同步构造语法树（Parse Tree）。每个解析函数返回对应语法单位的子树根节点（TreeNode）。TreeNode 使用嵌套的子节点列表表示树形结构，并通过递归打印方法（DFS 遍历）在控制台输出 Unicode 树形连接线的缩进格式。

**语法树输出示例**：

```
Program
├── int
├── main
├── (
├── )
├── {
├── StmtList
│   ├── Statement
│   │   └── Decl
│   │       ├── int
│   │       ├── id: a
│   │       └── ;
│   └── Statement
│       └── Assign
│           ├── id: a
│           ├── =
│           ├── Expr(+)
│           │   ├── Expr(*)
│           │   │   ├── 5
│           │   │   └── 2
│           │   └── 10
│           └── ;
└── }
```

## 五、语义分析和中间代码生成

### 5.1 语义分析和中间代码生成的任务

词法分析检查的是单个单词符号是否合法，语法分析检查的是单词序列是否满足文法结构。语义分析进一步检查源程序中静态语义的合法性，主要包括：

1. 变量是否先声明后使用（通过符号表检查）
2. 赋值语句左值是否为已声明的标识符
3. （完整编译器还需）类型相容性检查、数组下标越界检查等

中间代码是源代码与目标代码之间的桥梁，采用机器无关的表示形式。本设计选择**四元式**（Quadruple）作为中间代码的表示形式。

### 5.2 语义分析和中间代码生成的实现设计

**语法制导翻译方法**：在语法分析的同时进行语义检查和中间代码生成。具体来说，在递归下降分析的每个解析函数中，同步嵌入语义动作（类型检查、符号表操作）和代码生成动作（生成四元式）。

**四元式的格式**：`(op, arg1, arg2, result)`

- op：运算符（+、-、\*、/、=、j（无条件跳转）、j<（条件跳转）等）
- arg1：第一操作数（常量值、变量名或临时变量名 t1, t2, ...）
- arg2：第二操作数（单目操作时用 "_" 占位）
- result：结果变量名或跳转目标地址

**语义规则（产生式 → 语义动作）**：

| 产生式 | 语义动作 |
|--------|----------|
| Decl → int id ; | 将 id 加入符号表 symbolTable.add(id) |
| Assign → id = Expr ; | 检查 id 是否在符号表中；生成四元式 (=, Expr.val, \_, id) |
| Expr → Term ExprP | 传递属性值 |
| ExprP → + Term ExprP | 生成四元式 (+, left.val, right.val, temp) |
| ExprP → - Term ExprP | 生成四元式 (-, left.val, right.val, temp) |
| Term → Factor TermP | 传递属性值 |
| TermP → \* Factor TermP | 生成四元式 (\*, left.val, right.val, temp) |
| TermP → / Factor TermP | 生成四元式 (/, left.val, right.val, temp) |
| Factor → id | 返回标识符名（属性值为变量名） |
| Factor → num | 返回常数字符串（属性值为常量值） |
| Factor → ( Expr ) | 返回括号内表达式的属性值 |
| If → if ( E1 relop E2 ) { B } | 生成四元式 (j<反relop>, E1.val, E2.val, ?)；解析完 if 体后回填跳转地址 |
| While → while ( E1 relop E2 ) { B } | 记录 beginAddr；生成四元式 (j<反relop>, E1.val, E2.val, ?)；解析完循环体后生成 (j, \_, \_, beginAddr)；回填条件跳转地址 |

每个表达式的中间计算结果存入临时变量 t1, t2, t3, ...，通过 newTemp() 方法统一生成。

**语义分析的符号表设计**：

本设计实现的符号表（SymbolTable 类）用于管理变量声明，使用 HashSet 存储变量名，提供 O(1) 的插入与查询效率。

| 字段 | 说明 |
|------|------|
| 变量名集合 | 使用 HashSet\<String\> 存储已声明的标识符名称 |
| add(name) | 将新变量加入符号表（声明语句触发） |
| contains(name) | 检查变量是否已声明（赋值语句触发） |

在解析声明语句（`int a;`）时调用 `symbolTable.add("a")`，在解析赋值语句（`a = ...`）时先调用 `symbolTable.contains("a")` 检查。若变量未声明，则抛出错误"变量未声明"。

### 5.3 控制流语句的中间代码生成

**if 语句的中间代码结构**：

```
条件判断表达式
(j<=反relop>, left, right, ?)    ← 条件为假时跳转到 endAddr（暂填占位符）
if 体四元式...
endAddr:  ← 回填条件跳转的目标地址
```

使用**反向运算符**实现条件为假时跳转：对于 `if (a > b)`，生成的跳转指令是 `j<= a b target`，即 a<=b 时跳转到 target。反向关系为：> ↔ <=，< ↔ >=，>= ↔ <，<= ↔ >，== ↔ !=，!= ↔ ==。

**while 语句的中间代码结构**：

```
beginAddr:  条件判断表达式
            (j<反relop>, left, right, ?)   ← 条件为假时跳出循环（暂填占位符）
            循环体四元式...
            (j, _, _, beginAddr)              ← 无条件跳回循环头
endAddr:    ← 回填条件跳转的目标地址
```

**回填技术（Backpatching）**：生成条件跳转指令时，其目标地址暂时未知（因为循环体/条件体还没有解析），因此先填入占位符。等循环体/条件体解析完毕，目标地址确定后，再回填正确的四元式序号地址。

**中间代码生成示例**（以测试源码 `int a; a = 5 * 2 + 10;` 为例）：

```
原始四元式序列：
0: (*   , 5   , 2   , t1  )    // t1 = 5 * 2
1: (+   , t1  , 10  , t2  )    // t2 = t1 + 10
2: (=   , t2  , _   , a   )    // a = t2
```

至此，语义分析和中间代码生成的设计与实现叙述完成。

## 六、优化器的设计

### 6.1 优化任务

四元式生成后，需要在不改变程序运行结果的情况下对四元式进行优化。优化的目标是减少四元式数量和减少运行时计算，从而节省存储空间和运行时间。本次设计主要实现了两种经典优化技术：

**（1）常量合并（Constant Folding）**：在编译时计算常量表达式的值，而非生成运行时计算指令。

**（2）常量传播（Constant Propagation）**：如果一个变量在某个程序点是常数值，则将该值替换到后续使用处，从而触发更多的常量合并。

### 6.2 优化算法设计

优化算法采用**单遍扫描**方法，遍历四元式序列，同时维护一个常量映射表（constMap）记录临时变量的常量值。

算法流程（对每条四元式 q 依次执行）：

1. **常量传播**：检查 q.arg1 和 q.arg2 是否在 constMap 中，若存在则替换为对应的常量值
2. **常量合并**：若 arg1 和 arg2 都是数值常量，在编译时计算 op(arg1, arg2)。成功则将该指令替换为赋值指令 (=, 结果, _, result)，并将结果记入 constMap
3. **常量赋值记录**：若指令为 (=, 常量, _, 临时变量)，将 临时变量→常量 记入 constMap 中
4. **保留原指令**：不需要合并的指令保持原样（但操作数已做过常量传播替换）

核心实现代码：

```java
public static List<Quadruple> optimize(List<Quadruple> inputQuads) {
    List<Quadruple> resultQuads = new ArrayList<>();
    Map<String, String> constMap = new HashMap<>();  // 临时变量 → 常量值
    
    for (Quadruple q : inputQuads) {
        String arg1 = q.arg1, arg2 = q.arg2;
        // ① 常量传播
        if (constMap.containsKey(arg1)) arg1 = constMap.get(arg1);
        if (constMap.containsKey(arg2)) arg2 = constMap.get(arg2);
        // ② 常量合并
        if (isNumber(arg1) && isNumber(arg2)) {
            String val = calculate(q.op, arg1, arg2);
            if (val != null) {
                constMap.put(q.result, val);
                resultQuads.add(new Quadruple("=", val, "_", q.result));
                continue;
            }
        }
        // ③ 常量赋值记录
        if (q.op.equals("=") && isNumber(arg1) && q.result.startsWith("t"))
            constMap.put(q.result, arg1);
        // ④ 保留原指令
        resultQuads.add(new Quadruple(q.op, arg1, arg2, q.result));
    }
    return cleanUp(resultQuads);
}
```

### 6.3 优化效果示例

以测试源码 `a = 5 * 2 + 10` 为例：

| 步骤 | 优化前 | 优化后 | 说明 |
|:----:|--------|--------|------|
| 0 | (\*, 5, 2, t1) | (=, 10, \_, t1) | 常量合并：5\*2 编译时算出为 10 |
| 1 | (+, t1, 10, t2) | (=, 20, \_, t2) | 常量传播 t1→10 + 常量合并 10+10=20 |
| 2 | (=, t2, \_, a) | (=, 20, \_, a) | 常量传播 t2→20，a 直接赋值为 20 |

优化前需要 2 次运行时计算（乘法 5\*2、加法 t1+10），优化后全部在编译时完成，不产生任何运行时算术运算指令。安全处理除零情况：检测到除数为 0 则返回 null（不优化），保留原始指令交由运行时处理。

**后续可扩展的优化**：基本块划分（识别入口语句并分割基本块）、DAG 优化（在基本块内绘制有向无环图消除公共子表达式和死代码）、循环优化（代码外提、强度削弱、归纳变量消除）。这些优化因时间原因未在本次设计中实现，但在报告中给出设计思路。

## 七、目标代码生成器的设计思路（选做，未实现）

目标代码生成阶段以基本块为基础，对基本块中的四元式进行扫描并分析，通过查表的方式得到每个四元式对应的目标机器指令，最终将所有基本块生成的代码合并得到完整的目标代码。

本设计选择汇编语言作为目标代码。在考虑寄存器数量无限的前提下，从四元式翻译到汇编代码相对简单——每条四元式对应 1-3 条汇编指令。例如：

```
四元式:  (+, a, b, t1)    →    汇编:
                              MOV  AX, a
                              ADD  AX, b
                              MOV  t1, AX
四元式:  (=, t1, _, a)    →     MOV  a, t1
```

实际编译器面临的核心挑战是**寄存器分配**——四元式中使用了数量不限的变量和临时变量，而处理器拥有的寄存器是极其有限的。目前编译器使用的寄存器分配算法通常采用图着色法（近似最优分配），而本设计的朴素方案是将所有变量放在内存中，寄存器需要时读取、不需要时放回。

## 八、设计总结和心得

本次课程考核设计完成了一个简易 C/Java 子集编译器的前端设计，严格按照编译原理课程的五个阶段进行了实现：

1. **词法分析**：基于 DFA 状态转换图，从零实现了手写词法分析器，支持 15 个关键字、标识符、4 种常量类型（整数/浮点数/字符串/字符）、14 种运算符（含双字符运算符）、7 种界符的识别，以及注释跳过和词法错误检测。在此过程中深入理解了有限自动机与正则表达式之间的等价关系，以及最长匹配原则在工程实现中的体现。

2. **语法分析**：实现了 LL(1) 文法的完整理论计算（First 集、Follow 集、预测分析表 M 的矩阵化展示），同时用递归下降分析法完成了实际解析。这一过程让我深刻体会到自顶向下分析中"消除左递归"和"提取公共左因子"的必要性——如果不做这两步处理，递归下降分析将陷入无限循环或无法判定选择哪条产生式。

3. **语义分析与中间代码生成**：采用语法制导翻译方法，在语法分析过程中同步完成了符号表管理、变量声明检查、四元式生成和控制流回填。特别是回填（Backpatching）技术的实现，让我理解了单遍编译如何处理前向跳转——先生成占位指令、确定目标地址后再回填，这是一种简洁而高效的工程技术。

4. **代码优化**：实现了常量合并和常量传播两种经典优化。看似简单的一个 `5*2→10` 的替换，背后涉及的是编译器优化最核心的思想——在不改变程序语义的前提下，尽可能多地在编译时完成计算，以减少运行时开销。

通过本次编译器设计的实际操作，将编译原理课程中零散的知识点（DFA、LL(1)、FIRST/FOLLOW、语法制导翻译、四元式、基本块、常量优化等）串联成了一个有机的整体。从前只知道 IDE 写代码一键运行，现在终于理解了从源代码到可执行程序之间，编译器所经历的每一步转换和优化。

**技术亮点总结**：
- 完整的前端全流程（词法→语法→语义→中间代码→优化）
- LL(1) 理论计算与递归下降实际解析的双重实现
- 可视化的 HTML 报告生成（5 个交互式选项卡）
- 支持注释跳过、双字符运算符、转义字符、错误恢复等工程化特性

## 九、查重报告

（注：本报告中涉及的编译原理基础概念（如 DFA 的定义、FIRST/FOLLOW 集的构造算法、LL(1) 文法的判定方法）必然与教材和其他参考资料存在相似之处，这些是编译原理的标准知识。报告中的代码实现、测试用例、算法描述和心得体会均为本人的原创工作。）
